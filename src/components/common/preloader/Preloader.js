import preloader from "../../../img/preloader.svg";
import React from "react";


let Preloader = (props) => {
    return <div style={{display: 'inline-flex'}}><img src={preloader} /></div>
}

export default Preloader;